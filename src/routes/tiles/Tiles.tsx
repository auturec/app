import React from 'react';

import './Tiles.scss';

const Tiles: React.FC = () => {
  return (
    <div className="tiles">
      <p>This is the tiles page</p>
    </div>
  );
};

export default Tiles;
